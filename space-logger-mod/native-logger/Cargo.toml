[package]
name = "space-logger-native"
version = "0.1.0"
edition = "2024"

[lib]
name = "space_logger_native"
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"
once_cell = "1.21.3"
space-logger = { path = "../.." }
