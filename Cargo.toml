[package]
name = "space-logger"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "olap_bench"
harness = false
